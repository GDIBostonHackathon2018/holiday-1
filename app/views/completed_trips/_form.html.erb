<%= form_with(model: completed_trip, local: true) do |form| %>
  <% if completed_trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(completed_trip.errors.count, "error") %> prohibited this completed_trip from being saved:</h2>

      <ul>
      <% completed_trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
